[project]
name = "app"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "lib",
    "requests>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/app"]

[tool.uv.sources]
lib = { workspace = true }
